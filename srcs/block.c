#include "push_swap.h"
/*
void	identify_block(t_app *app)
{
	int i;
	int j;
	int temoin;
	t_elem *tmp;

	i = 0;
	j = 0;
	tmp = app->pre;
	temoin = tmp->current_nbr;
	tmp = tmp->pre_next;
	while (tmp)
	{
		if ((ABS(tmp->current_nbr - temoin)) > (int)app->nbr_nb * MAX_BLOCK)
		{
			temoin = app->statistic.data[j];
			i++;
		}
		tmp->block_nbr = i;
		tmp = tmp->pre_next;
		j++;
	}
}*/
